# Use the commands below to create a new ReactJS app

```sh
# required npm/nodeJS
npx create-react-app BoMTheBuilder

# Commit to this repo
git add .
git commit -m "initial commit"
git push origin main
```